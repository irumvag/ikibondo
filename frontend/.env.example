# Ikibondo frontend environment — copy to .env.local and adjust.

# Base URL of the Django REST backend (no trailing slash).
# Also used to derive the CSP connect-src in next.config.js.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
