import { WorkflowPlaceholder } from '@/components/workflows/WorkflowPlaceholder';

export default function Page() {
  return (
    <WorkflowPlaceholder
      title="Broadcast"
      description="Send camp-scoped push or SMS notifications to CHWs and parents."
      phase="Phase 5"
    />
  );
}
