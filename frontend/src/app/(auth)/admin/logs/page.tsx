import { WorkflowPlaceholder } from '@/components/workflows/WorkflowPlaceholder';

export default function Page() {
  return (
    <WorkflowPlaceholder
      title="System Logs"
      description="Full audit log viewer with filters by user, role, action, date range."
      phase="Phase 6"
    />
  );
}
