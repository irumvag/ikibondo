# Python
__pycache__/
*.py[cod]
*.pyo

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Raw / unprocessed data (processed synthetic CSVs are tracked)
data/raw/

# Trained model binaries — regenerate via ml/scripts/train_*.py.
# Only the diff-able *_metadata.json files are tracked (see models/).
*.pkl
*.joblib

# Local venv if someone creates one inside ml/
venv/
.venv/
